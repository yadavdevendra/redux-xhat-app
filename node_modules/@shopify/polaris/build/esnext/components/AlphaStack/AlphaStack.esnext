import { createElement } from 'react';
import { classNames, getResponsiveProps, sanitizeCustomProperties } from '../../utilities/css.esnext';
import styles from './AlphaStack.scss.esnext';

const AlphaStack = ({
  as = 'div',
  children,
  align = 'start',
  fullWidth,
  spacing = '4'
}) => {
  const className = classNames(styles.AlphaStack, fullWidth && styles.fullWidth, as === 'ul' && styles.listReset);
  const style = {
    '--pc-stack-align': align ? `${align}` : '',
    ...getResponsiveProps('stack', 'spacing', 'space', spacing)
  };
  return /*#__PURE__*/createElement(as, {
    className,
    style: sanitizeCustomProperties(style)
  }, children);
};

export { AlphaStack };
